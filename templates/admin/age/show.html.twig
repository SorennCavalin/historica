{% extends 'base.html.twig' %}

{% block title %}Age{% endblock %}

{% block body %}
    <h1>Age</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ age.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ age.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_age_index') }}">back to list</a>

    <a href="{{ path('app_admin_age_edit', {'id': age.id}) }}">edit</a>

    {{ include('admin/age/_delete_form.html.twig') }}
{% endblock %}
