<form method="post" action="{{ path('app_admin_age_delete', {'id': age.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ age.id) }}">
    <button class="btn">Delete</button>
</form>
